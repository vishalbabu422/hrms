import WorkOrderDesgnFormComponent from './form-component'

const Add = () => {
  const initialData = {
    workOrderId: '',
    deployments: [],
  }

  const handleSubmit = async (payload) => {
    try {
      const normalizedPayload = {
        ...payload,
      }

      await api.post('/admin/workorder-desgn/create', normalizedPayload)
      toast.success('Work created successfully')
      navigate('/empanelment')
    } catch (error) {
      console.error(error)
      toast.error(error.response?.data?.message || 'Failed to create Empanelment')
    }
  }

  return (
    <WorkOrderDesgnFormComponent initialData={initialData} mode="create" onSubmit={handleSubmit} />
  )
}

export default Add
