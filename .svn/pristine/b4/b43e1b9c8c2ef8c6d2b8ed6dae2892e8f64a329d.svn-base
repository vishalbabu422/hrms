const router = require("express").Router({ mergeParams: true });

const SalaryComponentController = require("./salaryComponent.controller");

const {
  protect,
  restrictTo,
  checkPermission,
} = require("../../../middlewares/authMiddleware");

const ROLES = ["ORG_ADMIN", "SUPER_ADMIN"];

router.use(protect);

router
  .route("/")
  .get(
    restrictTo(...ROLES),
    checkPermission("SALARY_COMPONENT.READ"),
    SalaryComponentController.getSalaryComponents
  )
  .post(
    restrictTo(...ROLES),
    checkPermission("SALARY_COMPONENT.CREATE"),
    SalaryComponentController.createSalaryComponent
  );

router
  .route("/:id")
  .get(
    restrictTo(...ROLES),
    checkPermission("SALARY_COMPONENT.READ"),
    SalaryComponentController.getSalaryComponentById
  )
  .patch(
    restrictTo(...ROLES),
    checkPermission("SALARY_COMPONENT.UPDATE"),
    SalaryComponentController.updateSalaryComponent
  )
  .delete(
    restrictTo(...ROLES),
    checkPermission("SALARY_COMPONENT.DELETE"),
    SalaryComponentController.deleteSalaryComponent
  );

module.exports = router;